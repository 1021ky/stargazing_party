# Design Document for Stargazing Party

## 概要

Stargazing Party は、星空観察に適した日付と周辺の宿泊施設を見つけるためのアプリケーションである。
主要な判断軸は「新月付近」と「晴天予報」であり、観測候補地の宿泊可否を同時に確認できることを目的とする。

## 背景

従来の中央寄せカード型レイアウトでは、地図操作と検索結果の比較閲覧が同時にしづらかった。
そのため、画面全体を使う左右分割レイアウトへ移行し、地図操作と結果閲覧を並行できるUIに刷新した。

今回の要望の中心は次の3点である。

- 100vh を使った画面固定レイアウト
- 地図中心の検索導線（都道府県セレクト依存を下げる）
- 今後の変更判断に使える仕様文書化

## 仕様（確定）

### 画面レイアウト

- ルートコンテナは `h-screen overflow-hidden`
- メイン領域は次のグリッド構成
- モバイル: `grid-rows-[54dvh_46dvh]`（上下2分割）
- `lg` 以上: `lg:grid-cols-2 lg:grid-rows-1`（左右2分割）
- 右ペインは `overflow-y-auto` により独立スクロール

### 左ペイン（地図 + 検索）

- 地図はペイン全体を使って表示
- 検索UIは地図上のフローティングパネルとして表示
- 都道府県のセレクトボックスは廃止
- 都道府県選択は地図クリック（PrefectureMapPicker）で行う
- 日付選択は既存の晴天候補ロジックを利用
- 価格・評価フィルタUIは存在するが、現時点では検索条件に未反映

### 右ペイン（検索結果）

- 宿泊施設カードを縦方向に表示
- カードグリッドは `grid-cols-1`、`xl` 以上で `xl:grid-cols-2`
- ソートは次の2種類のみ
- 価格が安い順（`price-asc`）
- 評価が高い順（`rating-desc`）

### API 契約

- 今回は UI 先行のため検索 API 契約は変更しない
- 現行の検索呼び出し（`date`, `prefecture`）を維持

## 非スコープ

- Bounds を使った検索 API 契約への変更
- 価格・評価フィルタの実検索反映
- 住所入力起点の検索導線追加
- 結果カードの3列化

## 判断基準

次回以降の変更は、以下を基準に判断する。

- UI先行で成立する改善は優先して実施する
- API 契約変更を伴う改善は、UIと分離して別タスク化する
- 右ペインの情報密度を上げる変更は、カード可読性を維持できる場合のみ採用する
- 地図中心導線に逆行する変更（都道府県セレクト再導入など）は原則採用しない

## 受け入れ基準

- 画面全体でブラウザの全体スクロールが発生しない
- `lg` 以上で左右2分割、モバイルで上下2分割になる
- 右ペインのみ独立して縦スクロールできる
- 地図上のパネルで日付選択と検索操作が可能
- 並び替えで「価格が安い順」「評価が高い順」が反映される
- 12-13インチ相当の画面で主要要素が重ならず利用できる

## 将来バックログ

- Bounds 連携による検索 API 拡張
- 価格・評価フィルタの実検索適用
- 住所入力からの検索導線追加
- 手動検証項目の強化（12-13インチ解像度での見え方確認を定常化）
